package Chap5.interfacess;

// an interface can extend multiple interfaces so it inherits all unimplemented interface methods
public interface MultInterface3  extends MultInterface1, MultInterface {

/*
 * Any class that implements the Seal interface must provide an implementation
 * for all methods in the parent interfaces—in this case
 */

}
