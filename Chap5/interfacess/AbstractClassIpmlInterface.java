package Chap5.interfacess;

public abstract class AbstractClassIpmlInterface implements MultInterface3{
    /*
     * AbstractClassIpmlInterface class implements MultInterface3 interface but is required
     * to provide cocncrete implementation of the abstract methods in the faces
     * 
     * if this class was not an abstract one, it would have to implement the abstract methods
     * 
     */
    
}
